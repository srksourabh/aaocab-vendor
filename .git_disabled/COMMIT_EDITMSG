feat: complete Phase V4 — Hindi i18n, offline support, loading skeletons, error handling

- Full Hindi translation coverage across all registration and dashboard pages
- OfflineIndicator in both registration and dashboard layouts
- Loading skeletons for register, dashboard, fleet, trips, earnings
- Error boundaries (global, register, dashboard) with Call Support fallback
- Font display swap and lazy-loaded AI components for performance
- VendorHeader and dashboard nav labels translated

Build: 15 routes, 0 TypeScript errors.
